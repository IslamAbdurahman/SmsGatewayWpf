<UserControl x:Class="SmsGatewayApp.Views.BlacklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:SmsGatewayApp.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Qora Ro'yxat (Blacklist)" Style="{StaticResource HeaderLabel}"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Blacklist List -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,20,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="BLOKLANGAN RAQAMLAR" FontSize="14" FontWeight="ExtraBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,15"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Blacklist}" SelectedItem="{Binding SelectedBlacklistEntry}"
                              AutoGenerateColumns="False" CanUserAddRows="False"
                              Background="Transparent" BorderThickness="0" 
                              Foreground="{StaticResource TextBrush}" FontSize="14" RowHeight="55">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="10,10,10,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#2AFFFFFF"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1AFFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Telefon (Phone)" Binding="{Binding Phone}" Width="150"/>
                            <DataGridTextColumn Header="Sabab (Reason)" Binding="{Binding Reason}" Width="*"/>
                            <DataGridTextColumn Header="Sana" Binding="{Binding AddedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                            <DataGridTemplateColumn Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" Background="Transparent" BorderThickness="0" Foreground="{StaticResource AccentBrush}"
                                                Command="{Binding DataContext.RemoveBlacklistCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" ToolTip="Olib tashlash"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Add to Blacklist -->
            <Border Grid.Column="1" Style="{StaticResource CardBorder}" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Yangi raqam qo'shish" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Text="Telefon raqam" Foreground="{StaticResource TextDimBrush}" FontSize="12" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding BlacklistPhone, UpdateSourceTrigger=PropertyChanged}" helpers:TextBoxHelper.Placeholder="+998901234567" Height="35" Margin="0,0,0,15"/>
                    <TextBlock Text="Sabab (ixtiyoriy)" Foreground="{StaticResource TextDimBrush}" FontSize="12" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding BlacklistReason, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,20"/>
                    <Button Content="Qo'shish" Style="{StaticResource DangerButton}" Command="{Binding AddPhoneToBlacklistCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
